# Frontend (Vite). Copy to .env.local only if you need overrides.
# Leave VITE_API_URL unset in dev: Vite proxies /api → http://127.0.0.1:5000 (no CORS issues).
# Set VITE_API_URL if the API is on another host/port, e.g.:
# VITE_API_URL=http://localhost:5000
